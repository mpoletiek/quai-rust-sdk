name: sdk
on:
  push:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: sdk-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  rust:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, macos-15, windows-2025]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - run: rustup toolchain install 1.97.1 --profile minimal --component rustfmt --component clippy
      - run: cargo fmt --all --check
        if: runner.os == 'Linux'
      - run: cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
      - run: cargo test --workspace --all-features --locked
      - run: cargo test --workspace --no-default-features --locked
      - run: cargo doc --workspace --all-features --no-deps --locked
        env:
          RUSTDOCFLAGS: -D warnings
  reference-and-harness:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - run: node --version && npm --version && python3 --version
      - run: npm ci --prefix compatibility --ignore-scripts
      - run: npm run verify --prefix compatibility
      - run: npm test --prefix compatibility
      - run: node --test crates/quai-browser/tests/*.test.mjs
      - run: npm run generate --prefix compatibility
      - run: node test-infra/generate_address_allocation_fixtures.mjs
      - run: git diff --exit-code -- test-infra/fixtures/address-allocation.json
      - run: node test-infra/generate_contract_code_fixtures.mjs
      - run: git diff --exit-code -- test-infra/fixtures/contract-code.json
      - run: node test-infra/generate_qi_custody_fixtures.mjs
      - run: git diff --exit-code -- test-infra/fixtures/qi-custody.json
      - run: node test-infra/generate_account_custody_fixtures.mjs
      - run: git diff --exit-code -- test-infra/fixtures/account-custody.json
      - run: node test-infra/generate_payment_allocation_fixtures.mjs
      - run: git diff --exit-code -- test-infra/fixtures/payment-allocation.json
      - run: node test-infra/generate_allocation_merge_fixtures.mjs
      - run: git diff --exit-code -- test-infra/fixtures/allocation-merge.json
      - run: npm run wrapping --prefix compatibility
      - run: git diff --exit-code -- crates/quai-consensus/tests/wrapping-vectors.json crates/quai-sdk/tests/wrapper-calls.json
      - run: python3 test-infra/sync_package_files.py --check
      - run: npm run parity --prefix compatibility
      - run: python3 test-infra/check_sdk_docs.py
      - run: git diff --exit-code -- compatibility/api-inventory.json compatibility/fixtures/
      - name: Verify Rust Qi message signing with the pinned JS backend
        run: |
          rustup toolchain install 1.97.1 --profile minimal
          cargo run -p quai-sdk --example qi_message --locked > "$RUNNER_TEMP/qi-message.json"
          node compatibility/scripts/verify-qi-message.mjs "$RUNNER_TEMP/qi-message.json"
      - name: Compile the isolated acceptance client without starting a node
        run: |
          cargo fetch --locked
          python3 test-infra/local-chain/highlevel_harness.py build
      - run: python3 -m unittest discover -s test-infra/tests -v
      - run: python3 -m unittest discover -s test-infra/local-chain -p 'test_*.py' -v
  browser:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - run: rustup toolchain install 1.97.1 --profile minimal --component clippy --target wasm32-unknown-unknown
      - run: cargo install wasm-bindgen-cli --version 0.2.128 --locked
      - run: cargo fetch --locked
      - run: cargo check -p quai-sdk --no-default-features --features browser,wallet,abi,payments,keystore,backup --target wasm32-unknown-unknown --locked
      - run: cargo clippy -p quai-browser --target wasm32-unknown-unknown --all-targets --locked -- -D warnings
      - name: Configure installed browser and driver
        run: |
          python3 - <<'PYTHON'
          import json, os, pathlib, shutil
          chrome = shutil.which('google-chrome') or shutil.which('chromium')
          driver = shutil.which('chromedriver')
          assert chrome and driver, 'Chrome and chromedriver are required'
          config = pathlib.Path(os.environ['RUNNER_TEMP']) / 'quai-webdriver.json'
          config.write_text(json.dumps({'goog:chromeOptions': {'binary': chrome, 'args': ['--headless=new', '--disable-dev-shm-usage']}}))
          with open(os.environ['GITHUB_ENV'], 'a') as output:
              output.write(f'CHROMEDRIVER={driver}\nWASM_BINDGEN_TEST_WEBDRIVER_JSON={config}\n')
          PYTHON
      - run: python3 crates/quai-browser/tests/run_storage.py
      - run: python3 crates/quai-browser/tests/run_browser.py
        env:
          CARGO_TARGET_WASM32_UNKNOWN_UNKNOWN_RUNNER: wasm-bindgen-test-runner
          WASM_BINDGEN_TEST_TIMEOUT: 90
      - run: python3 crates/quai-browser/tests/run_browser.py --suite worker
        env:
          CARGO_TARGET_WASM32_UNKNOWN_UNKNOWN_RUNNER: wasm-bindgen-test-runner
          WASM_BINDGEN_TEST_TIMEOUT: 90
      - run: python3 crates/quai-browser/tests/run_browser.py --suite sdk-worker
      - run: python3 crates/quai-browser/tests/run_browser.py --suite sdk-contract-code
      - run: python3 crates/quai-browser/tests/run_browser.py --suite sdk-contracts
      - run: python3 crates/quai-browser/tests/run_browser.py --suite sdk-events
      - run: python3 crates/quai-browser/tests/run_browser.py --suite sdk-human-abi
      - run: python3 crates/quai-browser/tests/run_browser.py --suite sdk-receipts
      - run: python3 crates/quai-browser/tests/run_browser.py --suite sdk-allocation-merge
        env:
          WASM_BINDGEN_TEST_TIMEOUT: 180
      - run: python3 crates/quai-browser/tests/run_browser.py --suite sdk-portable-capture
        env:
          WASM_BINDGEN_TEST_TIMEOUT: 180
      - run: python3 crates/quai-browser/tests/run_browser.py --suite sdk-qi-custody
        env:
          WASM_BINDGEN_TEST_TIMEOUT: 180
      - run: python3 crates/quai-browser/tests/run_browser.py --suite sdk-recovery
      - run: python3 crates/quai-browser/tests/run_browser.py --suite sdk-account-preflight
      - run: python3 crates/quai-browser/tests/run_browser.py --suite sdk-account-custody
      - run: python3 crates/quai-browser/tests/run_browser.py --suite sdk-account-backup
        env:
          WASM_BINDGEN_TEST_TIMEOUT: 180
      - run: python3 crates/quai-browser/tests/run_browser.py --suite sdk-keys
      - run: python3 crates/quai-browser/tests/run_browser.py --suite sdk-backups
        env:
          WASM_BINDGEN_TEST_TIMEOUT: 180
          CARGO_TARGET_WASM32_UNKNOWN_UNKNOWN_RUNNER: wasm-bindgen-test-runner
      - run: python3 crates/quai-browser/tests/run_browser.py --suite sdk-allocations
        env:
          WASM_BINDGEN_TEST_TIMEOUT: 180
      - run: python3 crates/quai-browser/tests/run_browser.py --suite sdk-payment-allocations
        env:
          WASM_BINDGEN_TEST_TIMEOUT: 180
  fuzz-smoke:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - run: rustup toolchain install nightly-2026-09-11 --profile minimal --component rust-src
      - run: cargo install cargo-fuzz --version 0.13.2 --locked
      - run: cargo fetch --manifest-path fuzz/Cargo.toml --locked
      - run: cargo +nightly-2026-09-11 fuzz build --fuzz-dir fuzz --no-cfg-fuzzing --codegen-units 16
        env:
          CARGO_NET_OFFLINE: true
      - run: python3 fuzz/run-smoke.py --seconds 30
  dependency-advisories:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - run: rustup toolchain install 1.97.1 --profile minimal
      - run: cargo install cargo-audit --version 0.22.2 --locked
      # Warnings remain visible. Two inactive optional-dependency maintenance
      # warnings are documented in docs/dependency-audit.md; none are ignored.
      - run: cargo audit
      - run: cargo fetch --manifest-path fuzz/Cargo.toml --locked
      - run: cargo audit --file fuzz/Cargo.lock
      - run: npm ci --prefix compatibility --ignore-scripts
      - run: npm audit --prefix compatibility --audit-level=low

  packaged-consumer:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - run: rustup toolchain install 1.97.1 --profile minimal --target wasm32-unknown-unknown
      - run: cargo fetch --locked
      - run: python3 test-infra/package_rehearsal.py --wasm --report "$RUNNER_TEMP/quai-package-rehearsal.json"
      - name: Show package validation log on failure
        if: failure()
        run: tail -100 "$RUNNER_TEMP/quai-package-rehearsal.log"
